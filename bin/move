#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
puts "Welcome to Tic Tac Toe!"
puts "Where would you like to go?"
move = gets.strip
input_to_index(move)
move(board, input_to_index(move)) 
display_board(move(board, input_to_index(move)) )